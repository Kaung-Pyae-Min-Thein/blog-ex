<%- include("components/header"); -%>
  <h1>Home</h1>

  <% blogs.forEach( blog=> { %>
    <h2>

      <%=blog.title %>
    </h2>
    <p>

      <%= blog.content.substring(0,100)+".........." %>
        <a href="/content/<%= blog.title %>">Read More</a>
    </p>
    <%})%>
      <%- include("components/footer"); -%>